import pytest
from game_manager import GameManager

def test_get_cpu_player_object_easy() -> None:
    # TODO: Figure out a way to mock inputs for both marker and ui.select_CPU_difficulty.
    # Although marker can probably just be provided in the function call, so no need to mock it.
    pass